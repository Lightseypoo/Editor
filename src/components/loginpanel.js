import React from 'react';

class LoginPanel extends React.component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>login panel</div>
        );
    }
}

export default LoginPanel;